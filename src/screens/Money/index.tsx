import { View, Text } from 'react-native';

export default function Money() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <Text> Faça suas tranferencias e depósitos </Text>
    </View>
  );
}
