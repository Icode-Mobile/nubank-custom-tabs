import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  Container: {
    alignItems: 'center',
    paddingTop: 40,
    paddingHorizontal: 0,
    paddingBottom: 30,
  },
  Top: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10,
  },
  Title: {
    fontSize: 18,
    color: '#fff',
    fontWeight: 'bold',
    marginLeft: 8,
  },
  Logo: {},
});
